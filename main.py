import json
import time
import sys

def main():
    # first process the json file into a dict
    file = open('test.json')
    data = json.load(file)
    

if __name__ == "__main__":
    sys.exit(main())